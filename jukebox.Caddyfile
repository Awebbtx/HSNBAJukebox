# Caddy auto-manages Let's Encrypt cert, HTTP->HTTPS redirect, and ACME challenges.
jukebox.hsnba.org {
    reverse_proxy 172.16.10.5:3000
}

reporting.hsnba.org {
    reverse_proxy 172.16.10.5:3001
}
